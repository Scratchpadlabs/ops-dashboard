{
  "//": "No \"functions\" section here on purpose: this repo's Cloud Functions (functions/generate_import, generate_invoice, etc.) are deployed one-by-one via plain `gcloud functions deploy`, not the Firebase CLI - see functions/DEPLOY.md. `firebase deploy` (this file) only ever touches hosting/firestore/storage; `firebase deploy --only functions:<name>` will fail here since no functions codebase is registered.",
  "hosting": {
    "public": "dist",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ],
    "headers": [
      {
        "source": "**",
        "headers": [
          { "key": "Cache-Control", "value": "no-cache" }
        ]
      }
    ]
  },
  "firestore": {
    "//": "firestore.indexes.json is NOT a complete picture of the project's composite indexes - it currently declares only the assessments(termId, subjectId) index the teacher app needs (docs/school-setup-page-spec.md 2). Before running `firebase deploy --only firestore:indexes`, reconcile it against the live set (`gcloud firestore indexes composite list --project clarified-1501`) and add anything missing, so the deploy cannot propose removing an index the teacher app depends on. Creating an index is additive and safe; removing one breaks a live query.",
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "storage": {
    "rules": "storage.rules"
  }
}
