# `..` is functions/, which is what makes `import shared.class_resolver` work
# here exactly as it does in the deployed tree, where tools/deploy_function.sh
# stages the shared package alongside this folder's modules.
[pytest]
pythonpath = . ..
testpaths = tests
